# Claude entrypoint

请先读取根目录 [AGENTS.md](AGENTS.md)，它是本仓库所有 agent 的共同规范与唯一协作约束来源。随后按其中顺序读 README.md、PROJECT_STATUS.md 和 docs/AGENT_TASKS.md。

本文件只做兼容入口，不维护第二套规则。默认由 Cursor CLI 实现代码，由协调者独立审计并在用户授权范围内集成。
