# 顶会强度论文战略 · 从「好 FYP」到「A 类主会」

> 配套 `RESEARCH_MASTER_PLAN.md`。本文件回答一个问题：
> **在现有方向上，能写出的最强形态的论文是什么？以及怎么写。**
> 撰写日期：2026-09-04

---

## Part 1 · 先想清楚：顶会论文和好 FYP 论文的差别不在工作量

一个常见误解是「多做实验 = 更好的论文」。不是。差别在 **claim 的结构**：

| | 好的 FYP 论文 | A 类主会论文 |
|---|---|---|
| 核心句式 | "我们**构建了**一个 X" | "我们**发现** P，因此 Q 是错的" |
| 贡献单位 | 系统组件 | 可证伪的命题 |
| 实验作用 | 展示系统能跑 | **检验命题**（可能失败） |
| 读者收获 | 知道有人做了这个 | 改变了对某件事的判断 |
| 审稿人问 | "工作量够吗" | "如果这是真的，我需要改变什么" |

**所以第一件事不是加实验，是找到一个"如果为真，别人必须改变做法"的命题。**

我们现在的方向（双视角 + 因果诊断）是**好 FYP**。下面是把它变成 **A 类命题**的路径。

---

## Part 2 · 最强形态：**处方式推荐是 performative 的**

### 2.1 一句话命题

> **给供给方的改进建议，其估计出的收益会因为建议被采纳而消失。**
> 因为在竞争集内部，质量是**相对的**——如果系统告诉一个竞争集里的所有酒店"去改善清洁度"，
> 清洁度的相对回报就会被竞争掉。**所有现存的供给侧诊断工具都隐含假设了回报是固定的。这个假设是错的。**

这个命题有三个好性质：
1. **可证伪**——回报是否随同集竞争者的同向改进而衰减，是一个可以估计的系数。估出来不显著，命题就错了。
2. **有后果**——如果为真，现在业界所有的 comp-set benchmarking 工具（STR、Revinate、TrustYou）给出的建议都是系统性高估的。
3. **有理论出身**——它是 performative prediction（Perdomo et al., ICML 2020）和 supply-side equilibria（Jagadeesan, Garg & Steinhardt, NeurIPS 2023）在真实市场里的实例化。

### 2.2 为什么这是一个真空

供给侧策略响应的文献 **全部是理论/仿真的**：

| 文献 | 做了什么 | 用什么数据 |
|---|---|---|
| Ben-Porat & Tennenholtz, NeurIPS 2018 | 策略性内容提供者的博弈论模型 | 理论 |
| Perdomo et al., ICML 2020 | Performative prediction 框架 | 理论 + 合成 |
| Eilat & Rosenfeld, ICML 2023 | Performative recommendation, 用激励做多样化 | 理论 + 半合成 |
| Jagadeesan, Garg & Steinhardt, NeurIPS 2023 | 供给侧均衡、内容专精化 | 理论 + 合成 |
| Hron et al. / PPA-Game / FORC 2026 | 创作者激励、学习算法诱导低质量 | 理论 + 仿真 |

**没有人测过。** 原因很实际：在 Spotify/YouTube 上，"内容向量"不可观测，供给方的改动也不可观测。

**酒店市场是这个理论的理想实验室**：
- 供给方的"内容向量" = 8 维 aspect 质量，**可以从评论文本随时间测出来**
- 供给方的改动 = aspect 质量的持续性跃迁，**可观测**
- 结果 = 竞争集内的需求份额，**可观测**
- 竞争集 = 天然的局部市场边界，**可构造且可验证**

> **这就是论文的位置：把一个纯理论的文献第一次落到真实市场数据上，并且发现理论预测的效应确实存在、量级多大。**

### 2.3 论文的三层 claim（层层递进，各自可证伪）

**L1（测量）**：评论情感 ≠ 质量。观测到的 aspect 情感混杂了**客群构成**（谁在住）与**真实质量**。
去混杂后，相当比例的"质量提升"消失。
→ *可证伪*：去混杂前后排名的 Kendall τ；被推翻的"改进事件"占比。

**L2（因果）**：用竞争集作为匹配对照，可以识别 aspect 改进对后续需求/评分的因果效应；
效应是**异质的**（取决于该店在竞争集里的位置和该 aspect 的市场显著度）。
→ *可证伪*：event study 的处理前系数应不显著；placebo 应无效应。

**L3（performativity）★ 核心**：该因果效应**随同集竞争者的同向改进单调递减**。
→ *可证伪*：peer adoption 的交互项系数应显著为负。**这一条不成立，论文就退回 L2 的强度。**

**L4（后果）**：因此，忽略 L3 的处方策略（"修你最差的一项"、"补最大 gap"）是系统性错配的；
一个 equilibrium-aware 的策略在离线回放和校准仿真里都更优，且**优势随采纳率上升而扩大**。

### 2.4 Figure 1（论文的命脉）

> **一张图定生死。** Figure 1 必须在读者读完 abstract 后 10 秒内让他相信 L3。

```
x 轴：同一竞争集内、同一时期改进了同一 aspect 的竞争者数量 (0,1,2,...,m)
y 轴：该 aspect 改进对自身需求份额的估计因果效应 τ̂ （带 95% CI）
预期：一条从正值单调下降、并在高 peer adoption 处穿过 0 的曲线
副图：随机对照组（假竞争集）应是一条平线 → 证明不是共同冲击造成的
```

如果这张图画出来是干净的下降曲线，这篇论文就成立了。**所有工程排期都应该服务于尽早画出这张图。**
建议：**S1 结束后立刻做一个粗糙版本的 Figure 1**（哪怕 ABSA 还没微调完，先用现成模型跑）。
这是整个项目的 **go / no-go 信号**，不要等到 S4 才知道。

---

## Part 3 · 技术皇冠：三个让审稿人点头的技术点

一篇 A 类论文通常有 1–2 个"哦，这个处理得漂亮"的技术点。我们有三个候选，全做。

### 3.1 用竞争集的**非传递性**识别 peer effect（最强的一个）

估 peer effect 会撞上 Manski 的 **reflection problem**：我的结果和同伴的结果互相决定，线性均值模型不可识别。

**但我们的竞争集是重叠的、非传递的**：A 的竞争集含 B，B 的竞争集含 C，A 的竞争集**不含** C。
这正是 Bramoullé, Djebbari & Fortin (2009) 证明的可识别条件——**网络的非传递性提供了排除限制**：
用「竞争者的竞争者」的特征作为「竞争者行为」的工具变量。

```
一阶段: peer_adopt[C(h), k, t] ~ Z[C(C(h))\C(h), k, t-1] + FE
        Z = 二阶邻居的历史 aspect 赤字（预测他们会改进，但不直接影响 h 的需求）
二阶段: y[h,t] ~ treat[h,k,t] × peer_adopt_hat + FE
```

> **这个点值得在论文里单独一小节 + 一个网络示意图。** 它把「我们的竞争集是个好构造」从产品论证变成了**计量经济学上的必要条件**——竞争集的重叠结构不是 bug，是识别策略的来源。这是全篇最优雅的地方。

### 3.2 分半样本工具变量，处理 aspect 测量误差

审稿人必问："你的 treatment 是 NLP 抽出来的，有噪声，经典测量误差会把系数拉向 0。"

**便宜且干净的解法**：把每个 (hotel, quarter) 的评论**随机分成两半**，分别算 aspect 情感 → 得到同一潜在质量的**两个独立测量** →
用其中一半作为另一半的工具变量。经典 ME 校正，不需要额外数据，只需要一次重跑。

报告：naive 系数 vs 分半 IV 校正后的系数，差距即衰减偏误的量级。**这会直接堵住一整类审稿意见。**

### 3.3 客群构成去混杂（L1 的实现）

```
s[h,r,k,t] = q[h,k,t] + γ_k' x_r + δ[k,t] + ε
  s = 评论 r 对酒店 h 在 aspect k 的情感
  x_r = 评论者协变量（国籍、出行类型、同行人构成、停留时长、该评论者历史评论数、是否移动端提交）
  q = 我们要的**去混杂质量**（这是所有下游分析的输入）
估计: 双向固定效应 / 分层贝叶斯（q 上加 AR(1) 平滑，处理稀疏）
```

**为什么重要**：现有的酒店评论挖掘文献**全部**是直接平均情感。如果我们能展示"去混杂后有 X% 的改进事件消失、竞争集排名的 Kendall τ 只有 0.6X"，
这本身就是一个对整个应用领域的方法学警告——**是一个可以独立成段的发现**。

---

## Part 4 · 把领域从「酒店」抬到「市场」：加第二个 vertical

**这是提升论文层级性价比最高的一步。**

单一 vertical（酒店）的论文，审稿人会说"这是个 hospitality 应用"。两个 vertical，就变成"这是关于**市场中的供给侧处方**"。

**推荐加入 Yelp Open Dataset（餐厅）**，理由：
- 规模大、时间跨度长（2005–2022），有 `business_id`, `latitude`, `longitude`, `categories`, `stars`, `review_count`
- **自带价格档位** `attributes.RestaurantsPriceRange2`（1–4 档）→ **彻底绕开我们最大的痛点 P2（价格）**
- 有 `tip`、`checkin` 等额外需求信号
- 竞争集定义天然成立：地理 + 菜系 category + 价格档
- 学术上被广泛使用，reviewer 熟悉，不需要解释数据合法性

有了 Yelp，论文的主张变成：
> "我们在两个独立的本地服务市场（酒店 / 餐厅）、三个数据源、九个城市上，都观测到了处方回报随同伴采纳而衰减。"

**这句话的说服力比任何单一市场的精致分析都强。**

---

## Part 5 · 稳健性军火库（每一件都对应一类审稿意见）

| 审稿意见 | 我们的弹药 |
|---|---|
| "评论到达数不是需求" | 与平台自报 `Total_Number_of_Reviews` 增量交叉验证；用评分、评论量、平台评论数三个 outcome 都跑一遍；讨论平台排序作为未观测中介 |
| "aspect 抽取有噪声" | gold set F1 + HotelRec 子评分外部效度 + **分半 IV**（§3.3） |
| "peer 改进不是随机的" | city×time FE 吸收共同冲击 + **二阶邻居 IV**（§3.1）+ 随机竞争集 placebo |
| "平行趋势不成立" | event study 前置系数图 + placebo-in-time + Callaway–Sant'Anna 交错处理估计器 |
| "存在未观测混杂" | **Oster (2019) δ 边界** / Rosenbaum 敏感性分析——报告"要推翻结论需要多强的未观测混杂" |
| "COVID" | D1 (2015–2017) 天然规避；D3 跨 COVID 仅作稳健性且显式建模断点 |
| "只是相关不是因果" | 三种识别策略（DiD / IV / DR）结论一致才写因果，不一致就诚实降级 |
| "仿真不是证据" | 明确把仿真定位为"**已估参数的后果推演**"，不是证据来源 |
| "和 WSDM'22 有何不同" | 差异表 + **把它复现成基线**（最强的差异化是"我们比它好，好在哪"） |
| "基线调得不够" | 每个基线报告超参搜索空间与最优点；这是 token 预算最该花的地方之一 |

---

## Part 6 · 一百亿 token 具体买什么（转化清单）

Token 买不到：真实 A/B 测试、真实部署、人类被试规模、新数据的物理采集速度。
Token 能买到的，按**每 token 的论文收益**排序：

**第 1 档（直接决定接收与否）**
1. **基线的忠实复现与充分调参**——DeepCoNN / NARRE / ANR / WSDM'22 因果法 / BPR / ItemKNN，每个跑完整超参网格。论文被拒最常见的技术原因就是"基线没调好"。
2. **三数据源 × 两 vertical 的全量复制**——每一个 headline 结果都在 D1/D2/D3/Yelp 上重跑。
3. **稳健性矩阵全跑**——Part 5 的每一行 × 每个数据集，做成附录里的大表。

**第 2 档（决定论文的质量感）**
4. **大规模 LLM 标注 + 不确定性**——3 个模型家族 × 2 万句，只保留三方一致的样本作训练集；**人工只做 500 句**做最终 gold。这把"标注质量"从瓶颈变成非问题。
5. **敏感性曲面而非单点**——竞争集半径 × m × 权重 × δ 阈值 × 窗口长度，报告成曲面图，不是一个数。
6. **校准仿真**——采纳率 0→100% 的市场演化，多组参数。

**第 3 档（决定论文读起来像不像 A 类）**
7. **对抗性自审**——用 agent 扮演 3 个不同背景的审稿人（IR / 计量经济 / 推荐系统），各写一份 review，然后逐条回应并改稿。**重复 3 轮以上。** 这是 token 最划算的用法之一。
8. **文献真读 100+ 篇**——不是摘要，是读方法和实验设置。Related Work 的质量是 B 会和 A 会最明显的分水岭。
9. **写作迭代**——abstract 至少改 10 版，intro 至少 5 版。

**第 4 档（锦上添花）**
10. 可视化打磨、复现包、benchmark 文档。

> **反面清单**：不要用 token 去堆更多的模型变体（"我们又试了 Transformer 版本"）。
> 审稿人不会因为你多了一个模型而接收，只会因为你的命题更可信而接收。

---

## Part 7 · Benchmark 释放（一个独立的接收理由）

命名并释放一个 benchmark，让论文即使某个结果不够强也有被接收的理由：

**`PRESCRIBE`：供给侧处方推荐基准**
- **任务定义**：给定 provider 的历史状态与其竞争集，在预算约束下输出一组属性干预，最大化未来 H 期的实现收益
- **数据**：去混杂质量面板（hotel/restaurant × quarter × aspect）、竞争集图、人工标注 gold set
- **评测**：off-policy replay（IPS/SNIPS/DR）+ 平行趋势诊断 + 采纳率仿真
- **基线**：fix-worst / fix-largest-gap / most-mentioned / random / LLM-direct / equilibrium-aware(ours)

RecSys 有 reproducibility track，SIGIR/CIKM 有 resource track——**benchmark 本身可以是第二篇论文**。

---

## Part 8 · Abstract 初稿（先写它，再写别的）

> 写论文的正确顺序是：**Figure 1 → Abstract → Introduction → 实验 → 其余**。
> Abstract 写不出来，说明命题还没想清楚，此时做实验是浪费。

```
推荐系统正越来越多地同时服务消费者与供给方，而供给方最需要的服务是处方式的：
我应该改进哪一项？现有系统的回答是描述性的——指出该供给方相对竞争者落后的维度——
这隐含假设了改进的回报是该供给方的固有属性。我们证明这个假设是错的，
且错的方式会系统性地误配处方。

基于 [N] 条评论、[M] 家供给方、[K] 个城市、两个本地服务市场（酒店与餐厅）与三个数据源，我们
(i) 通过对客群构成去混杂，恢复了随时间变化的八维属性质量，发现 [X]% 的表观"质量提升"
在去混杂后消失；
(ii) 以本地竞争集为匹配对照识别属性改进的因果回报，并通过 event study 与 placebo 检验；
(iii) 利用重叠竞争集的非传递性识别同伴效应，发现每多一位同集竞争者在同期改进同一属性，
自身回报下降 [A]%——即处方式推荐是 performative 的。

这一发现有直接后果：被部署的处方系统会侵蚀它自己估计出的回报。我们把竞争下的处方
形式化为一个分配问题，提出 equilibrium-aware 策略，并用时序留出的离线回放与校准的
市场仿真评测。该策略较"修最差项"等描述性启发式获得高 [B]% 的实现收益，
且优势随采纳率上升而扩大。我们公开质量面板、竞争集图、人工标注与评测框架，
作为供给侧处方推荐的基准 PRESCRIBE。
```

标题候选：
1. **Prescriptive Recommendation is Performative: Returns to Provider Quality Investment under Competition**
2. Who Should Improve What? Equilibrium-Aware Prescription in Two-Sided Marketplaces
3. The Prescription Externality: Measuring Supply-Side Advice in Recommender Marketplaces

---

## Part 9 · Introduction 的五段式（A 类论文的标准骨架）

1. **P1 — 场景与转向**：推荐系统从单边走向多边；供给方侧最有价值的不是曝光公平，而是"我该改什么"。（2–3 句 + 一个真实场景）
2. **P2 — 现状与其隐含假设**：现有做法（业界 comp-set benchmarking、学术 aspect 诊断）都是描述性的，隐含"回报固定"。**明写这个假设**——审稿人喜欢看到有人把别人没说出口的假设挖出来。
3. **P3 — 为什么这个假设可疑，以及为什么难验证**：相对质量是零和的；但理论文献无法验证，因为内容向量不可观测。**这一段要写"为什么这件事难"**——难度论证是 A 类论文的必备件。
4. **P4 — 我们做了什么 + 关键发现**：三个数字（去混杂推翻比例、因果效应量、peer 衰减率）。**在 intro 就把数字给出来。**
5. **P5 — 贡献列表 + 一句后果**：4 条贡献，最后一句写"如果我们的发现成立，供给侧诊断工具需要重新设计"。

---

## Part 10 · 预演审稿（现在就写，不要等投稿）

### Reviewer A（IR / RecSys 背景）
- "这更像一篇计量经济学论文，推荐系统的成分在哪？"
  → **回应准备**：必须有 Tourist 侧的排序实验，且要展示 L3 如何改变**排序系统本身**的设计（equilibrium-aware policy 是一个推荐策略）。**这是 Tourist 侧存在的真正理由**——不是为了"双视角"好听，是为了把命题落回推荐系统。
- "数据不是工业规模。"
  → 两个 vertical、九个城市、百万级评论；且强调这是**首个可观测供给方内容向量的真实市场**。

### Reviewer B（计量经济 / 信息系统背景）
- "reflection problem 怎么解决的？" → §3.1
- "测量误差？" → §3.2
- "未观测混杂？" → Oster 边界
- "review arrival ≠ demand" → 三 outcome + 交叉验证 + 明确 limitation

### Reviewer C（怀疑一切型）
- "performativity 效应会不会只是均值回归？"
  → **必须准备**：处理组是"高于自身基线 δ 个标准差的跃迁"，天然有均值回归风险。
  用 Ashenfelter dip 检验 + 以基线水平为条件的匹配 + 对未处理组做同样的"伪跃迁"检验。**这是最危险的一条批评，提前打掉。**
- "8 个 aspect 是不是拍脑袋？" → 用 D2 的 TripAdvisor 子评分做外部锚定；报告 aspect 集合的敏感性

> **执行建议**：每完成一个 Stage，就让 agent 扮演这三位审稿人写一份 review，然后修。至少三轮。

---

## Part 11 · 分层目标（诚实的期望管理）

| 层级 | 内容 | 产出 | 现实概率 |
|---|---|---|---|
| **Core** | L1 去混杂 + 竞争集验证 + L2 因果效应 + ABSA gold set | 扎实 FYP + ENTER/workshop 论文 | 高（只要按 CODE_PLAN 走） |
| **Strong** | + L3 peer effect + L4 策略评测 + 双视角耦合 | **RecSys / CIKM 正会** | 中（取决于 Figure 1 是否成立） |
| **Stretch** | + 第二 vertical (Yelp) + 均衡仿真 + benchmark 释放 + 分半 IV + Oster 边界 | **SIGIR / WWW / KDD 有一搏** | 偏低但值得试 |

**关键的分叉点是 Figure 1**。所以：**尽早做粗糙版 Figure 1**，用它决定后面几个月怎么投入。

如果 L3 检验出来是 null（回报没有被竞争掉），**不要硬编**。那本身是一个有意思的负结果：
"在本地服务市场中，质量投资的回报并未被竞争迅速侵蚀，这与供给侧均衡理论的预测不符，
可能的原因是 [容量约束 / 改进的持久性 / 需求的地理黏性]"——
这依然可发（强度约等于 Strong 层的下限），但必须**从一开始就把论文写成"我们检验了 X"而不是"我们证明了 X"**。

---

## Part 12 · 修订后的时间线（以 RecSys 2027-04-25 为主目标）

| 时间 | 阶段 | 关键动作 | 判定 |
|---|---|---|---|
| 2026-09 | S0 数据底盘 | D1 + Yelp 接入；价格代理 | 有效竞争集 ≥ 50 |
| 2026-10 上旬 | S1 竞争集 | 构建 + T1/T2 证伪 | T2 显著 |
| **2026-10 下旬** | **★ 粗糙版 Figure 1** | 用现成 ABSA 跑一版 L3 | **go / no-go 决策点** |
| 2026-11 | S2 ABSA | gold set + LoRA + 外部效度 | F1 ≥ 0.75 |
| 2026-12 | S1.5 去混杂 | L1 客群构成调整 | 报出被推翻的改进事件比例 |
| 2027-01 | S4a 因果主结果 | DiD + event study + placebo | 无预趋势 |
| 2027-02 | S4b performativity | 二阶邻居 IV + 分半 IV + Figure 1 正式版 | L3 系数显著 |
| 2027-03 上旬 | S5 策略与仿真 | Policy Value + 采纳率仿真 | 优于全部基线 |
| 2027-03 下旬 | Tourist 侧 + 耦合 | 排序实验 + H1 | 显著 |
| 2027-04 | 写作 + 三轮对抗自审 | abstract 改 10 版 | 外部读者 ≥ 2 |

> 注意这个排期把 **Tourist 侧后置**了。原因见 Part 10 的 Reviewer A：
> Tourist 侧的作用是把命题落回推荐系统，它是**服务于主命题的配件**，不是主线。
> 先把主命题做出来，再补配件。

---

## Part 13 · 与 FYP 交付物的关系

**论文和 FYP 论文不是同一份东西，不要混。**

- **FYP thesis**：面向导师和答辩，要完整、要有系统实现、要有教学性的背景铺陈。Tourist 侧、系统架构、界面截图都该写。
- **会议论文**：面向审稿人，只讲一个命题，其余全部砍掉进附录。

建议：**先写会议论文（4 月投），再把它扩写成 FYP thesis（5 月交）**。反过来会写出一篇臃肿的会议论文。
