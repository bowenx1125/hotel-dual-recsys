# 当前项目状态 · 2026-09-13

最新研究基线为 `f45ba59`（2026-09-11），已与 Demo 独有截图提交 `0f5ebd5` 合并到 `main`。本次整理只整合版本、迁移本地资产、修复迁移路径和更新入口，不改研究算法、门槛或既有实验结果。

## 已实现

| 内容 | 落地 | 当前证据 |
|---|---|---|
| 酒店经理诊断 UI | `demo/app.py`、`demo/scoring.py` | 诊断与行动分开，位置不可直接改进；Manager 页面 24 家酒店快照、22 家可展示 |
| 全量研究测量 | `src/autonomous/panel.py`、`common.py` | 515,738 条评论、1,493 家酒店、6 城市；89,075 个面板单元，80,686 有测量、8,389 无测量 |
| 方面标签可靠性审查 | `src/autonomous/absa.py` | 2,800 对弱标签一致率 84.607%，κ=0.7159；仍需人工 gold |
| 本地参照酒店构建 | `src/autonomous/infer.py` | 1,476 家有地理参照支持；kNN、半径和随机参照比较；未证明经济替代性 |
| 时序变化与预测 | `src/autonomous/infer.py` | 456 个严格交叉拟合事件，涉及 315 家酒店；预测仅 PARTIAL，事件研究 WEAK |
| 离线行动策略表 | `outputs/autonomous/wave6/` | 1,485 家酒店；已生成 weakest / criticized / reliability 策略列与 abstention 标记；不能把元数据列出的所有基线当成均已完成严格对比 |
| 可追溯报告与草稿 | `run_research.py`、`paper/autonomous/`、`outputs/autonomous/` | 分波执行、断点、事实文件、声明表、负结果、工作论文草稿 |

## 为什么采用这些方法

评论少的酒店直接取情感均值会不稳定，所以使用对称 Beta-Binomial 贝叶斯收缩；没有评论提及的方面保留缺失标记，避免把未知误当中性。分析只使用完整季度，检测变化与评估结果使用不同评论折，降低时间泄漏和同一批评论自证的风险。地理参照酒店提供相近情境下的比较，但不自动等于真实竞争关系。经理建议需要考虑是否能执行、测量是否足够，所以位置保留在诊断层，行动层选择可操作方面，Demo 对证据不足给出拒绝建议。

## 创新候选与研究边界

当前最有依据的贡献方向是 Track B：把“可操作性”和“测量可靠性”纳入供给方推荐，将评论诊断转化为受约束的行动建议，并显式记录不确定性和拒绝建议。这是问题建模与系统方法组合上的贡献方向，尚未经过完整文献新颖性核验或强基线验证，不能宣称首创算法。原 Track A 的同伴干扰/竞争拥挤收益命题尚未得到确认性支持。

同伴特征使末折 MAE 从 0.370384 降至 0.369371，但酒店聚类 bootstrap 的差值 95% CI 为 [-0.001369, 0.003541]，跨零。456 是严格主分析事件数；5,774 只属于旧宽松规则。敏感性分析另有不同统计口径，不应替换主结果。ABSA 84.61% 是与弱标签一致率，不能写成人工标注准确率。当前仍为 WORKING_PAPER_ONLY，游客侧排序、真实管理干预、预订/收益评估未完成。

## 下一步

1. 完成人工 ABSA gold 标注，优先检查弱标签分歧及早餐等较弱方面。
2. 将拒绝建议规则与各个离线策略输出统一，补足真正可比较的策略基线和经理评估；现有 abstention 标记不能替代整套决策策略验收。
3. 把 515K 离线策略与 Manager UI 对齐，再评估扩展后的覆盖率和可用性。
4. 若继续因果/收益方向，需要真实翻修、经理行动或预订日志；游客排序需独立数据标签与评测。

本次迁移验证结果见整理记录；以上研究数值是已保存产物，不是本次重新计算的结果。
