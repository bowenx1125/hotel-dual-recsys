# 研究计划：双视角酒店推荐系统

本文件是完整的研究蓝图。Claude Code 在开始任何一个 Phase 前应阅读对应章节。

---

## 0. 背景与动机

旅游者面对海量酒店和评论难以决策；酒店经理缺乏工具理解自己在同类竞争中的真实位置。现有酒店推荐研究：
- 绝大多数是 **Tourist 单视角**（给用户推酒店）。
- 处理价格时多用**绝对价格过滤**，忽略"价格感知是地区相对的"（同样"中等价位"，布鲁塞尔和海岸小镇含义不同）。
- 对评论的利用多停留在整体情感或评分预测，**很少把差评拆解成可执行的 aspect 诊断**。

**本研究的定位**：用一套共享底层表征，同时服务旅游者和经理两个视角，并把"性价比"和"差评"做成竞争集内可量化、可解释的指标。

---

## 1. 三个核心创新点（论文的 contribution）

### 创新点 ①：对称多利益相关者框架
同一底层表征（见 Phase 3），两个读取方向：
- Tourist：给定预算档位 → 该档位内综合最优排序。
- Manager：给定某家酒店 → 在其竞争集内的相对位置 + 诊断 + 建议。
> Novelty：把推荐系统"反过来用"，Manager 侧的竞争诊断在学术 RecSys 中少见。

### 创新点 ②：本地竞争集（Local Comp Set）建模
竞争集 = 客人在预订时会互相替代、同时考虑的那批酒店（consideration set）。用「地理 + 价格档位（+ 星级）」定义，而非绝对价格阈值。

### 创新点 ③：Aspect 级竞争诊断 + 差评情感分析
- 对评论（尤其差评）做 aspect-based sentiment，得到每家酒店的 aspect 质量向量 + 差评热点分布。
- Manager 诊断示例："你在你的竞争集里评分排第 4，差评 63% 集中在隔音和卫生，而排名靠前的竞争者差评主要在早餐——卫生/隔音是拖累你的关键，且这两项更影响复购。"

---

## 2. 关键方法论（务必理解，否则实现会出错）

### 2.1 两类维度必须分开
| 类别 | 作用 | 包含 |
|---|---|---|
| **定义维度** | 决定"谁和谁竞争"（划竞争集边界） | 地理位置、价格档位、星级/类型 |
| **评判维度** | 决定"在竞争集内谁更好"（排名/诊断） | 评级、评论数、差评数、差评情感、装修时间、地铁/景区距离 |

**为什么**：若把"评级"放进竞争集定义，则高分酒店只和高分酒店比，就永远发现不了"某家酒店在它的价位+位置段里定价虚高/被低估"。质量是评判的**结果**，不能当分组的**前提**。

### 2.2 价格的双重角色
- 粗粒度「价格档位」（分位数：低/中/高）→ 用来**分组**（定义维度）。
- 细粒度「价格相对位置」（在竞争集里贵/便宜多少%）→ 用来**评判**（评判维度）。

### 2.3 差评分析的两个层次（应对单店评论稀疏）
评论中位数仅 17 条/店，单店差评太少，统计不稳。因此：
- **竞争集层面聚合**：把同一竞争集所有酒店的差评汇总，得到该细分市场的共性痛点 → 稳健的统计结论。
- **单店 case study**：挑评论多的几家（如布鲁塞尔大酒店）做详细差评诊断 → 论文的可解释性案例。

---

## 3. 实现 Pipeline（Phase 0–7）

### Phase 0 — 数据补全（爬虫）
- **目标**：对 821 个 `hotel_url` 补全：价格（统一查询条件）、经纬度、星级、装修年份。
- **输出**：`data/scraped/hotel_attributes.csv`，以 `hotel_url` 为 key，可 join 回原数据。
- **细节**：见 `SCRAPING_SPEC.md`。
- **降级**：价格爬不到的酒店，用星级或"从评论文本抽取的价格感知"作为弱代理，并在论文标注。

### Phase 1 — 竞争集构建（创新点 ②）
- **输入**：补全后的酒店属性（经纬度、价格、星级）。
- **方法**：
  1. 地理聚类：同城内用距离半径（先试 1km，做敏感性分析）或 DBSCAN，把邻近酒店聚成地理组。
  2. 价格分档：在每个地理组内按价格分位数切 3 档（低/中/高）。
  3. （可选）星级/类型收紧。
  4. comp set = 同地理组 ∩ 同价格档（∩ 同星级）。
- **过滤**：comp set 内酒店数 < N（建议 N=5）的组标记为"无效竞争集"，不参与 Manager 分析。
- **输出**：每家酒店所属的 comp set id；`data/processed/compsets.csv`。
- **要做**：报告每个城市形成了多少个有效竞争集、平均规模——这是论文里证明 scope 合理的关键表。

### Phase 2 — Aspect 提取（创新点 ③）
- **固定 aspect 集**：位置(location)、卫生(cleanliness)、早餐(breakfast)、服务(service/staff)、隔音(noise/quiet)、房间(room)、性价比(value)。（可在 EDA 后微调，但要全程一致。）
- **方法**：
  - 对每条评论做 aspect-based sentiment（aspect → {正/负/未提及} 或情感分数）。
  - 工具选择：先试预训练 ABSA 模型（免费、可批量、不耗订阅额度）；若效果不足再考虑 LLM（注意成本，且批量 LLM 调用用 API key 而非订阅）。
  - **差评专门处理**：对负面评论（rating 低或情感判负）单独统计 aspect 分布。
- **输出**：
  - 每家酒店的 aspect 质量向量。
  - 每家酒店的差评热点分布（差评里各 aspect 占比）。
  - `data/processed/aspect_features.csv`。
- **验证**：人工抽查 N 条评论的 aspect 标注准确性，报告抽查一致率。

### Phase 3 — 双视角共享底层表征（创新点 ①）
- 每家酒店统一表征向量：
  `[价格相对位置, aspect质量向量, 位置便利性, 评论可信度权重(随评论数增长), 差评热点分布]`
  - 位置便利性：用经纬度算到最近地铁站/主要景区的距离（OpenStreetMap POI）。
  - 评论可信度权重：评论数少的酒店其评分不确定性高，需降权（如贝叶斯收缩到竞争集均值）。
- **输出**：`data/processed/hotel_representation.csv`。这是 Tourist 和 Manager 共用的底层。

### Phase 4 — Tourist 侧（创新点 ①）
- **输入**：用户预算档位（low/mid/high）。
- **输出**：该档位内酒店的综合 value 排序。
- **模型**：LightGBM learning-to-rank（或先做加权打分 baseline）。
- **Ground truth（relevance label）**：用高 `avg_rating` / 高评论数作为"好酒店"信号；可结合 tags 里的住宿满意信息。
- **注意**：明确特征集，避免标签泄漏（不要把直接等价于标签的字段当特征）。

### Phase 5 — Manager 侧（创新点 ① + ③）
- **输入**：某家酒店。
- **输出**（结构化诊断报告）：
  1. 在 comp set 内的综合排名 + 各 aspect 排名。
  2. 价格相对位置（比竞争集中位贵/便宜多少%）。
  3. 差评热点 vs 竞争集差评热点对比。
  4. 可执行建议（定价方向、优先改进的 aspect）。
- **方法**：底层用 Phase 3 表征算出量化位置；诊断文本用 LLM 基于结构化数值生成（保证可解释、不幻觉——把数值喂给 LLM，让它解释，而不是让它编数据）。

### Phase 6 — Evaluation
- **Tourist 侧（有 ground truth）**：
  - 指标：NDCG@K、Precision@K、Recall@K。
  - Baseline 对比：① 纯评分排序 ② 纯价格过滤+评分 ③ 传统协同过滤 ④（可选）纯 LLM 推荐。
- **Manager 侧（无直接 ground truth）**：
  - LLM-as-judge 评估生成建议的质量（相关性、可执行性、是否有数据支撑）。
  - Case study：选 2-3 家酒店做详细诊断展示。
  - （可选）人工评估：请几位评估者对诊断的合理性打分。
- **Ablation study（证明每个创新点有用）**：
  - 去掉差评 aspect 分析 → 看 Tourist 排序和 Manager 诊断质量下降。
  - 去掉价格相对位置（改用绝对价格）→ 看效果变化。
  - 去掉竞争集（全局排序）→ 看 Manager 侧是否失去意义。

### Phase 7 — 论文写作
建议结构：
1. Introduction（动机：双视角缺口 + 价格相对性 + 差评未被充分利用）
2. Related Work（评论推荐、aspect 情感、多利益相关者推荐、酒店定价）
3. Method（竞争集定义、共享表征、双视角）
4. Experiments（数据、baseline、指标、ablation）
5. Case Study（Manager 诊断实例）
6. Limitations & Future Work（诚实写 scope、数据偏比利时、价格快照近似、装修年份覆盖率）
7. Conclusion

> Related Work 是 B 会被拒重灾区——写作阶段回到 Chat，让 Claude 帮你梳理和定位差异。

---

## 4. Scope 与 Limitation（论文要诚实写出）
- 数据集偏比利时（地域单一），结论的泛化性需谨慎声明。
- 价格用"统一条件下的当前快照"近似历史评论期价格——只用于**相对档位**，已论证可接受，但要明说这个 assumption。
- 装修年份覆盖率取决于爬取，缺失部分作为 future work。
- 单店评论稀疏，故差评统计在竞争集层面做，单店仅作 case study。

---

## 5. 风险与应对
| 风险 | 应对 |
|---|---|
| Booking 反爬导致价格爬不到 | 优先抓 JSON-LD（经纬度/星级较易）；价格降级为代理；见 SCRAPING_SPEC |
| 有效竞争集太少 | 收紧/放宽地理半径做敏感性分析；必要时只聚焦布鲁塞尔 |
| ABSA 准确率不足 | 人工抽查；换模型；或对关键 case 用 LLM 复核 |
| 时间不足（一周） | 优先打通 Brussels 单城的完整 pipeline，再扩展其他城市 |

---

## 6. 一周执行顺序建议
1. Day 1–2：Phase 0 爬虫（脚本自己跑，不耗额度）+ Phase 1 竞争集
2. Day 2–3：Phase 2 aspect 提取（批量脚本跑）
3. Day 3–4：Phase 3 表征 + Phase 4 Tourist ranker
4. Day 4–5：Phase 5 Manager 诊断 + Phase 6 评测/ablation
5. Day 5–7：Phase 7 写作（回 Chat 协作）+ 图表
